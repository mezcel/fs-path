#!/bin/sh

## !!! under construction !!!
## Not sure what the best way to prompt file input is yet.

## This script will import local audio files into the server

## I probably should just link the files instead of copy.
## The spirit of this project is to setup an impromptu ad-hoc repo, so it wont be likely the server would have local audio files.
## If so... that should probably be handles from within the Golang server file management.